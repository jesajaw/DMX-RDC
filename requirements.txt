pyserial>=3.5
PyAudioWPatch>=0.2.12
numpy>=1.26
pillow>=10.0  # für Cover-Art-Rendering (Kreis-Maske, Rotation) in Music Mode
pywin32>=306  # optional, nur Fallback für Titel/Interpret ohne NowPlayingBridge.exe (Windows-only)
